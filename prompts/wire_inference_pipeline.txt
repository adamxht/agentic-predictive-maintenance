Now i want to wire up the inference pipeline, which is the deployed code to production. The inference config shall be set in /mnt/c/Users/User/workspace/agentic-predictive-maintenance/configs/data_transformation/default.yaml, consisting of how real time data will be preprocessed using the preprocessed/ artifacts + the feature enginerring code similar to the pipeline -> steps in test set. But only take what is needed, for example test_set_ingestion is not needed. Wire up the inference code inside src/pipeline/inference_pipeline.py. Then, expose the inference pipeline through app/api.py using fast api. Finally, use app/streamlit.py to demo the inference pipeline, simulating real time data with the test set data. treat each cycle as 10 seconds, just for demo purposes. The point is to show how we design the api structure with clean and stateless design.  For the UI, only show the engine IDS: 75, 25, 26 since those had the lowest errors. The UI should simulate real world production system so include logging and saving the data into a database with columns in the selected features + engine id and cycle. Add a bar in the UI indicating the model's prediction of the life ratio in the given , cycle and also plot the featrures in a graph without scaling them to see their actual values. The goal of the design is also to enable an LLM Agent to monitor data drift and diagnose the issue through tools like plots and text-to-sql on the real time database. Therefore there will also be a button to simulate a drift, for example make one of the feature's distribution all 0, and cause shap values for that feature to drop, the LLM will plot the shap and tell the data scientists and provide a recommendation. But leave out the LLM part out, just KIV. For this prompt, focus on the regular model inference pipeline and the streamlit UI. Remember to act as a staff data scientist and refer to AGENTS.md to ensure your code is of good quality. Grill me with questions until you fully understand the task.