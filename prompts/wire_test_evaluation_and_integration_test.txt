Now I want to run test set evaluation. Previously in our data preparation stage, we did not prepare the test set at all.  So we need to prepare them in the same way as in pipeline -> steps must be the same, but some steps are not applicable to test set, for example train_validation_split. So Add another field in the default.yaml specifically for the test set. Please refer to the raw data and the step3 notebook to see what the test set looks like, and how it was prepared in the notebook. After that, you may wire up the evaluation using the `/mnt/c/Users/User/workspace/agentic-predictive-maintenance/scripts/run_test_set_eval.py` as the entry point, make sure to reuse as much code as possible in the src/ only add new code if there is a test set specific implementation. Finally, add the test set evaluation as a integration test, for the integration test, you will need to run data preparation -> model training -> test set evaluation. But DO NOT read  or save anything from disk, everything should be ran in memory so that the CICD machine has no dependency. You can  create two Integration test, one for XGBoost and one for Ranfom Forest, take the best hyperparameters foudn by optuna, hardcode it in the test file, and run it once to get a baseline, then hardcode the baseline inside the file as well. In the integration test, instead of generating synthetic data, Use the actual test set itself, assuming the test set will be pushed to github as a git lfs. The purpose of the integration test is to gatekeep the model before a release, so it should be representative of the actual performance of the model. Remember to act as a staff data scientist and refer to AGENTS.md to ensure your code is of good quality. Grill me with questions until you fully understand the task.