Now we are ready to move on to the model training pipeline. The concept is similar to the data preprocessing, Refer to /mnt/c/Users/User/workspace/agentic-predictive-maintenance/notebooks/step3_modeling_life_ratio.ipynb For the flow. Also start with a config, the config should note down what model to train, the hyperparameters settings for optuna to study, evaluation metrics. For now, just add XGBoost and RandomForest just like the notebook, since these models use the sklearn api, we do not need to modify anything inside src/models. Add a model factory class to select the model based on the config given. I have added an entry ppint script for model training: /mnt/c/Users/User/workspace/agentic-predictive-maintenance/scripts/run_model_training.py. Give an option for users to add plot the validation error / training error into a training_logs/<descriptiverun_name>/plots. Write the plotting logic inside /mnt/c/Users/User/workspace/agentic-predictive-maintenance/src/plots.py. Refer to the notebook on the plots, also include shap values in the plot. Finally wire up MLFlow to enable experiment tracking and model registry. Key things to track are the dataset used, model configurations, validation metrics, training metrics.  The metrics should also include precision, recall and roc-auc, refer to the bottom of the notebook on how these are computed. Make sure the roc auc curve are plotted as well. The accuracy should be similar to what is shown in the notebook, but not necessarily the exact same. Add unit tests as well similar to the data preprocessing pipeline. After you have flushed both the run_model_training.py and unit test, update the README.md. Remember to act as a staff data scientist and refer to AGENTS.md to ensure your code is of good quality. Grill me with questions until you fully understand the task.